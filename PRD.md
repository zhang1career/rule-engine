# 规则引擎系统产品需求文档

## 1. 项目概述

### 1.1 项目背景
规则引擎系统是一个基于SpringBoot的规则计算服务，用于根据事件类型和用户参数执行相应的规则序列，并返回计算结果。

### 1.2 技术栈
- **框架**: SpringBoot
- **JDK版本**: JDK 8
- **构建工具**: Maven 3.6
- **脚本引擎**: Groovy

## 2. 功能需求

### 2.1 核心接口

#### 2.1.1 接口定义
- **接口路径**: `/rule/eval`
- **接口名称**: `eval`
- **调用方式**: 
  - HTTP（初期实现）
  - RPC（后续扩展）
- **调用模式**:
  - 同步阻塞（初期实现）
  - 同步非阻塞（后续扩展）
  - 异步非阻塞（后续扩展）

#### 2.1.2 请求参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| userId | Long | 是 | 用户ID，长整型 |
| eventId | Integer | 是 | 事件类型ID，用于区分不同的事件类型，整型 |
| traceId | Long | 是 | 业务请求追踪ID，长整型 |
| dataMap | Map<String, TypedValue> | 是 | 参与规则计算的参数字典，参数名作为key，value是TypedValue类型 |

**TypedValue类规范**:
- 提供 `getValue()` 方法，用于获取参数值
- 支持多种数据类型的封装

#### 2.1.3 响应结果

- **返回类型**: `TypedValue`
- **HTTP响应**: TypedValue实例的JSON序列化
- **TypedValue类规范**:
  - 提供 `getValue()` 方法，用于获取计算结果

### 2.2 规则执行引擎

#### 2.2.1 执行流程
1. 根据 `eventId` 获取对应的执行序列
2. 按照执行序列的顺序执行规则
3. 规则执行过程中支持：
   - 根据规则内部逻辑判断提前跳出顺序执行
   - 循环执行
   - 规则之间的相互调用
   - 递归调用

#### 2.2.2 规则类型
规则支持以下操作类型：
- **逻辑表达式**: 解析和执行逻辑表达式
- **接口查询**: 调用外部HTTP/RPC接口获取数据
- **SQL查询**: 执行数据库查询操作
- **脚本执行**: 执行Groovy脚本

#### 2.2.3 规则管理
- **规则标识**: 每个规则有唯一的 `ruleId`
- **执行序列**: 每个 `eventId` 对应一个执行序列
- **序列组成**: 执行序列包含若干规则，执行顺序由序列设定和管理
- **规则复用**: 一个规则可以在多个执行序列中使用

### 2.3 规则状态控制

#### 2.3.1 状态类型
规则状态分为以下四种：

| 状态 | 说明 | 执行条件 |
|------|------|----------|
| 下线 | 规则不可用 | 不允许加入到任何eventId对应的执行序列中 |
| 测试 | 测试环境规则 | 只允许测试环境请求的eventId触发执行 |
| A/B测试 | A/B测试规则 | 只允许生产环境请求的eventId触发执行，根据userId进行均匀化打散后，按设定比例决定是否执行 |
| 全量 | 生产环境规则 | 只允许生产环境请求的eventId触发执行 |

#### 2.3.2 A/B测试规则
- **触发条件**: 生产环境请求
- **分流逻辑**: 根据userId进行均匀化打散，按照设定的比例决定是否进入A/B测试规则
- **持久化**: 记录userId、eventId和ruleId的对应关系
- **一致性**: 只要规则仍处于A/B测试状态，该用户的该事件类型都会触发该规则的执行

### 2.4 消息队列集成

#### 2.4.1 消息发送
- **触发时机**: 规则计算完成后
- **消息内容**: 包含入口参数和计算结果
- **消息队列**: RabbitMQ（外部系统，通过接口方式调用）
- **发送方式**: 异步发送，不影响主流程

## 3. 非功能性需求

### 3.1 扩展性设计
- **接口扩展**: 预留RPC接口扩展点
- **调用模式扩展**: 预留同步非阻塞、异步非阻塞扩展点
- **规则类型扩展**: 支持新增规则类型

### 3.2 性能要求
- 支持高并发请求处理
- 规则执行效率优化

### 3.3 可维护性
- 代码结构清晰，模块化设计
- 支持规则配置化管理
- 完善的日志记录

## 4. 技术实现要点

### 4.1 架构设计
- **分层架构**: Controller层、Service层、规则引擎层、数据访问层
- **接口抽象**: 定义统一的规则接口，支持多种规则类型实现
- **执行引擎**: 实现规则序列执行引擎，支持流程控制

### 4.2 数据模型
- **规则模型**: Rule（ruleId, ruleType, ruleContent, status等）
- **执行序列模型**: ExecutionSequence（eventId, ruleIds, executionOrder等）
- **A/B测试记录**: ABTestRecord（userId, eventId, ruleId等）

### 4.3 配置管理
- 规则配置存储（建议使用数据库或配置中心）
- 执行序列配置管理
- A/B测试比例配置

## 5. 开发计划

### 5.1 第一阶段（MVP）
- [x] 项目基础架构搭建
- [ ] HTTP同步阻塞接口实现
- [ ] 基础规则执行引擎
- [ ] 规则状态控制（下线、测试、全量）
- [ ] RabbitMQ消息发送

### 5.2 第二阶段
- [ ] A/B测试功能实现
- [ ] 规则类型扩展（逻辑表达式、接口查询、SQL查询、Groovy脚本）
- [ ] 规则执行流程控制（循环、规则调用、递归）

### 5.3 第三阶段
- [ ] RPC接口实现
- [ ] 同步非阻塞调用方式
- [ ] 异步非阻塞调用方式

## 6. 接口示例

### 6.1 HTTP请求示例
```json
POST /rule/eval
{
  "userId": 123456789,
  "eventId": 1001,
  "traceId": 987654321,
  "dataMap": {
    "amount": {
      "value": 1000.00,
      "type": "DECIMAL"
    },
    "age": {
      "value": 25,
      "type": "INTEGER"
    }
  }
}
```

### 6.2 HTTP响应示例
```json
{
  "value": true,
  "type": "BOOLEAN"
}
```

