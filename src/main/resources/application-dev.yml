# Development environment configuration
rule:
  engine:
    environment: DEV

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rule_engine?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: zhang
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

# Development logging configuration (overrides main application.yml)
logging:
  level:
    root: DEBUG
    lab.zhang.rule: DEBUG
    # Third-party library logs
    com.baomidou.mybatisplus: INFO
    org.springframework.jdbc: DEBUG
    org.springframework.data.redis: DEBUG
    org.springframework.web: DEBUG

